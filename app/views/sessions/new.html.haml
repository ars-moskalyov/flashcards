= simple_form_for :session, :url => sessions_path do |f|
  = f.input :email, label: t('views.user.email'), error: false
  = f.input :password, label: t('views.user.password'), error: false
  = f.button :submit, t('views.session.sign_in')
